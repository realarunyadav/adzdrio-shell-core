# Preview backend configuration

The preview frontend must use the externally hosted NestJS API.

Required frontend environment variable:

`VITE_API_BASE_URL=https://ominous-space-sniffle-5vvrpr6w44j5hwg7-3000.app.github.dev`

Do not commit secrets. The preview environment should inject this variable through the hosting environment.
